# RAG API - Справочник Яндекса по ML

FastAPI сервис для поиска и генерации ответов на основе справочника Яндекса по машинному обучению. Является компонентом системы [Lifelong Learning Assistant](../README.md).

# Разработчик сервиса

Данный компонент системы разработал полностью Максим Пискаев ([theycallmemax](https://github.com/theycallmemax)).

Примечание: 
- данный сервис изначально был частью Github-репо ([ссылка](https://github.com/Lifelong-Learning-Assisttant/lifelong_learning_assistant)).
- позже Антон Ширяев ([medphisiker](https://github.com/medphisiker)) для удобства и единообразия выделил его в отдельный Github-репо и подключил к основному репо как `git submodule`.
- исходные коммиты от Максима Пискаева ([theycallmemax](https://github.com/theycallmemax)) можно посмотреть в дереве git в Github-репо ([ссылка](https://github.com/Lifelong-Learning-Assisttant/lifelong_learning_assistant)).


## 🎯 Как работает сервис

### Архитектура RAG Pipeline

```
┌─────────────────────────────────────────────────────────────┐
│                  RAG Pipeline с HyDE                         │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  1. Пользовательский запрос                                  │
│     ↓                                                         │
│  2. [Опционально] HyDE: генерация гипотетического ответа    │
│     ↓                                                         │
│  3. Embedding запроса (OpenAI text-embedding-3-large)        │
│     ↓                                                         │
│  4. Гибридный поиск в Qdrant (Dense + Sparse BM25)          │
│     • Поиск по child chunks (маленькие фрагменты)           │
│     • Фильтрация по порогу скора                            │
│     ↓                                                         │
│  5. Извлечение parent chunks из Redis                        │
│     • Полные документы для контекста                         │
│     • Дедупликация по parent_id                             │
│     ↓                                                         │
│  6. Формирование контекста с метаданными                     │
│     ↓                                                         │
│  7. Генерация ответа через LLM (GPT-4o-mini)                │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

### Ключевые технологии

**🔍 Поиск:**
- **Parent-Child стратегия**: маленькие child chunks для точного поиска, большие parent chunks для контекста.
- **Гибридный поиск**: комбинация dense (OpenAI) и sparse (BM25) embeddings.
- **HyDE (опционально)**: генерация гипотетического документа для улучшения поиска.

**💾 Хранилище:**
- **Qdrant**: векторная БД для child chunks (2071 документов).
- **Redis**: key-value хранилище для parent chunks.

**🤖 LLM:**
- **Embeddings**: OpenAI text-embedding-3-large (3072 dimensions).
- **Generation**: GPT-4o-mini для генерации ответов.
- **llm_service**: единый интерфейс для работы с провайдерами (OpenAI, OpenRouter, Mistral).

### Что такое HyDE?

**HyDE** (Hypothetical Document Embeddings) — техника улучшения поиска:
1. LLM генерирует гипотетический ответ на запрос.
2. Этот ответ используется для поиска вместо исходного запроса.
3. Гипотетический ответ ближе к реальным документам в векторном пространстве.

**Когда использовать HyDE:**
- ✅ Сложные технические вопросы
- ✅ Абстрактные концепции
- ✅ Когда обычный поиск дает плохие результаты
- ❌ Простые фактические вопросы (медленнее и дороже)

## 🚀 Как запустить

### 1. Настройка

```bash
# Создать .env файл
cp env.template .env

# Добавить ваш OpenAI API ключ в .env
nano .env  # или любой редактор
```

В `.env` обязательно укажите:
```env
OPENAI_API_KEY=sk-your-key-here
```

Все переменные окружения используют префиксы для группировки:
- `APP_*` - настройки приложения
- `OPENAI_*` - настройки OpenAI
- `QDRANT_*` - настройки Qdrant
- `REDIS_*` - настройки Redis
- `RAG_*` - настройки RAG (включая HyDE и порог скора)

### 2. Запуск

```bash
# Запустить все сервисы через Docker
docker compose up -d --build
```

### 3. Загрузка данных (только первый раз!)

Если базы данных пусты, выберите один из вариантов наполнения:

#### Вариант А: Загрузка из исходных Markdown-файлов (ETL)
Используйте этот способ, если хотите запустить весь процесс обработки текста и создания эмбеддингов заново.

Запустите `Jupyter notebook` в `notebook/rag_etl_loader.ipynb`

В интерфейсе Jupyter откройте файл и выполните:
```
Cell -> Run All
```

#### Вариант Б: Быстрое развертывание из бэкапа (рекомендуется)
Скачивание готовых снапшотов Qdrant и дампов Redis. Это значительно быстрее, чем полная пересборка индекса.

```bash
# Запустить утилиту скачивания (нужен Docker)
docker run --rm -v $(pwd)/rag/backups:/backups \
  ghcr.io/lifelong-learning-assisttant/rag-backup-downloader:v001
```
Подробную инструкцию по восстановлению из этих файлов см. в [Backup Downloader](./backup_downloader/README.md) и [Qdrant Migration](./docs/qdrant-migration.md).

### 4. Проверка

```bash
uv run python tests/evaluate_rag.py
```

## 📡 Эндпоинты API

### Поиск документов

**Обычный поиск:**
```bash
curl -X POST "http://localhost:8000/search" \
  -H "Content-Type: application/json" \
  -d '{"query": "что такое градиентный бустинг?", "top_k": 5}'
```

**Поиск с HyDE (для сложных запросов):**
```bash
curl -X POST "http://localhost:8000/search" \
  -H "Content-Type: application/json" \
  -d '{"query": "что такое градиентный бустинг?", "top_k": 5, "use_hyde": true}'
```

### Генерация ответа (RAG)

**Обычный RAG:**
```bash
curl -X POST "http://localhost:8000/rag" \
  -H "Content-Type: application/json" \
  -d '{"query": "Объясни градиентный спуск", "top_k": 5}'
```

**RAG с HyDE:**
```bash
curl -X POST "http://localhost:8000/rag" \
  -H "Content-Type: application/json" \
  -d '{"query": "Объясни принцип работы attention в трансформерах", "top_k": 5, "use_hyde": true}'
```

### Python пример

```python
import requests

response = requests.post(
    "http://localhost:8000/rag",
    json={"query": "Что такое переобучение?", "top_k": 5}
)

print(response.json()["answer"])
```

## 📊 Оценка качества (Evaluation)

В проекте настроена система оценки качества ответов:
- **Ragas**: Ноутбук [`notebook/ragas_evaluation.ipynb`](./notebook/ragas_evaluation.ipynb) позволяет оценить Faithfulness, Answer Relevance и другие метрики.
- **Metrics**: Ноутбук [`notebook/rag_metrics_evaluation.ipynb`](./notebook/rag_metrics_evaluation.ipynb) для сравнения качества поиска.
- **Datasets**: В папке [`tests/`](./tests) содержатся наборы вопросов и эталонных ответов.

## 🔗 Полезные ссылки

| Сервис | URL |
|--------|-----|
| API Docs | http://localhost:8000/docs |
| Qdrant UI | http://localhost:6333/dashboard |
| Redis UI | http://localhost:8081 |

## 🛠️ Управление сервисами

```bash
docker compose up -d --build  # Запустить и собрать
docker compose down           # Остановить
docker compose restart rag-api # Перезапустить API
docker compose logs -f rag-api # Логи API
docker compose ps             # Статус
```

## 🐳 Сервисы

| Сервис | Порт | Описание |
|--------|------|----------|
| RAG API | 8000 | FastAPI сервис |
| Qdrant | 6333 | Векторная БД |
| Redis | 6379 | Key-Value хранилище |
| Redis Commander | 8081 | UI для Redis |

## 🔧 Разработка

### Локальный запуск API (без Docker)

```bash
# Запустить только инфраструктуру
docker compose up -d qdrant redis redis-commander

# Запустить API локально
uv run uvicorn app.main:app --reload
```

### Пересборка после изменений
```bash
docker compose up -d --build rag-api
```

## 📁 Структура

```text
.
├── app/                  # FastAPI приложение
│   ├── main.py           # Роуты и запуск
│   ├── config.py         # Настройки Pydantic-settings
│   ├── retriever.py      # Логика поиска (Hybrid, HyDE)
│   └── rag_service.py    # Сервис генерации ответа
├── backup_downloader/    # Утилита для скачивания бэкапов БД
├── llm_service/          # Модуль для работы с разными LLM
├── notebook/             # ETL процессы и оценка (Ragas)
├── tests/                # Тесты и датасеты для оценки
├── pyproject.toml        # Зависимости (uv)
└── settings.py           # Глобальные настройки провайдеров
```

## ❓ Проблемы?

### "Коллекция не найдена"
Запустите ETL процесс в Jupyter ([`notebook/rag_etl_loader.ipynb`](./notebook/rag_etl_loader.ipynb)) или скачайте бэкап.

### Сервисы не запускаются
```bash
docker compose down
docker compose up -d
```

### API не отвечает
Проверьте логи: `docker compose logs -f rag-api`.

## 🤖 Использование в AI-агентах

```python
# Tool definition для OpenAI
tools = [{
    "type": "function",
    "function": {
        "name": "search_ml_handbook",
        "description": "Поиск в справочнике Яндекса по ML",
        "parameters": {
            "type": "object",
            "properties": {
                "query": {"type": "string", "description": "Вопрос"}
            },
            "required": ["query"]
        }
    }
}]

# Реализация
def search_ml_handbook(query: str) -> str:
    response = requests.post(
        "http://localhost:8000/rag",
        json={"query": query}
    )
    return response.json()["answer"]
```

## 📊 Технологии

- **FastAPI** - REST API
- **LangChain** - RAG пайплайн
- **Qdrant** - Векторная БД (hybrid search)
- **Redis** - Хранилище parent chunks
- **OpenAI** - Embeddings & LLM
- **BM25** - Sparse embeddings
- **Ragas** - Фреймворк для оценки RAG

## ⚙️ Настройки RAG

### Переменные окружения

```env
RAG_RETRIEVAL_TOP_K=10              # Количество документов для поиска
RAG_MAX_CONTEXT_TOKENS=4000         # Максимум токенов в контексте
RAG_USE_HYDE_BY_DEFAULT=false       # Использовать HyDE по умолчанию
RAG_SCORE_THRESHOLD=0.7             # Порог скора (меньше = строже)
```

### Рекомендации по порогу скора

Порог скора фильтрует результаты по качеству (distance метрика: меньше = лучше):

- **0.4-0.5** - Строгая фильтрация (только высококачественные результаты)
- **0.6-0.7** - Сбалансированный подход (рекомендуется) ✅
- **0.8-0.9** - Максимальное покрытие (больше результатов, ниже качество)

### Когда использовать HyDE

**Используйте HyDE (`use_hyde: true`) для:**
- Сложных концептуальных вопросов
- Технических объяснений
- Абстрактных тем
- Когда обычный поиск дает плохие результаты

**Не используйте HyDE для:**
- Простых фактических вопросов
- Поиска по конкретным терминам
- Когда важна скорость (HyDE делает дополнительный LLM вызов)

## 📄 Лицензия

MIT
